# Backline Inference API - base tier.
#
# Enough to run the server in STUB_MODE with real audio analysis. No torch, no
# model downloads, installs in well under a minute.
#
#   py -3.11 -m venv .venv
#   .venv\Scripts\Activate.ps1
#   pip install -r requirements.txt
#
# For real generation and transcription, see requirements-gpu.txt.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2
python-multipart==0.0.9
httpx==0.27.2
soundfile==0.12.1
numpy==1.26.4
librosa==0.10.2.post1
